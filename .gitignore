node_modules/
dist/
src-tauri/target/
src-tauri/gen/schemas/
.DS_Store
*.log

# Release artifacts
*.dmg

# Secrets — never commit
.env
.env.*
!.env.example

# Temporary PDF render trace (see PdfView).
.workbench-pdf-debug.log

# Your research library, not source. Papers are downloaded from publishers and
# their full text is extracted into the notes beside them — neither belongs in
# a code repository, and republishing a paper is not ours to do.
papers/
